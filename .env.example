# --- mode: demo | test | live ---
APP_MODE=demo
PORT=3000
HOST=127.0.0.1
BASE_URL=http://localhost:3000
DATA_DIR=./data

# --- payments (test/live only) ---
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# --- fax provider (test/live only) ---
# TODO: chosen after TERMS-DILIGENCE.md is resolved (Phaxio or Telnyx Fax).
# Do NOT put real keys here until the provider's white-label/partner terms
# are confirmed in writing. Live mode additionally requires
# FAX_AUTHORIZATION_REFERENCE (see src/config.js).
FAX_PROVIDER=
FAX_API_KEY=
FAX_API_SECRET=
FAX_WEBHOOK_SECRET=
FAX_AUTHORIZATION_REFERENCE=

# --- business identity (live only) ---
SUPPORT_EMAIL=
LEGAL_BUSINESS_NAME=
BUSINESS_ADDRESS=
BUSINESS_NAME=FaxSend

# --- ops ---
TRUST_PROXY=
