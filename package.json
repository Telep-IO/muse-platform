{
  "author": "",
  "dependencies": {
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "multer": "^2.4.0",
    "pdf-lib": "^1.17.1",
    "pg": "^8.23.0",
    "stripe": "^22.6.2",
    "zod": "^4.6.5"
  },
  "description": "Collect e-signatures through an agent: draft an envelope, review, pay $2.99, send for signing",
  "engines": {
    "node": ">=24"
  },
  "keywords": [],
  "license": "ISC",
  "name": "signsend",
  "private": true,
  "scripts": {
    "check": "node --check src/server.js && node --check src/app.js && node --check src/envelopes.js && node --check src/providers.js && node --check public/app.js && node --check src/approval.js && node --check src/store.js && node --check src/config.js && node --check src/documents.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "start": "node --env-file-if-exists=.env src/server.js"
  },
  "type": "module",
  "version": "0.1.0"
}
