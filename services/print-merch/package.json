{
  "name": "print-merch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PrintMerch fulfillment: Stripe payment, then Printify production.",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.23.0",
    "stripe": "^18.5.0"
  }
}
